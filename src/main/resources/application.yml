spring:
  application:
    name: work-kg-backend
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 110MB
  datasource:
    url: jdbc:postgresql://postgres-duve.railway.internal:5432/railway
    username: postgres
    password: KCLQJyHRjAzTHZvJJuRpQpnRFBmBaTJo
    driver-class-name: org.postgresql.Driver
    hikari:
      # Максимальное количество соединений в пуле
      maximum-pool-size: 20

      # Минимальное количество простаивающих соединений
      minimum-idle: 5

      # Максимальное время ожидания соединения (30 секунд)
      connection-timeout: 30000

      # Максимальное время жизни соединения (30 минут)
      max-lifetime: 1800000

      # Время простоя соединения перед проверкой (10 минут)
      idle-timeout: 600000

      # Время ожидания валидации соединения (5 секунд)
      validation-timeout: 5000

      # SQL запрос для проверки соединения
      connection-test-query: SELECT 1

      # Автоматический коммит
      auto-commit: true

      # Имя пула (для логов)
      pool-name: WorkKG-HikariPool

      # Регистрация JMX для мониторинга
      register-mbeans: true

      # Изоляция соединений (только чтение)
      # read-only: false

      # Дополнительные свойства для PostgreSQL
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 250
        prepStmtCacheSqlLimit: 2048
        useServerPrepStmts: true
        useLocalSessionState: true
        rewriteBatchedStatements: true
        cacheResultSetMetadata: true
        cacheServerConfiguration: true
        elideSetAutoCommits: true
        maintainTimeStats: false

  jpa:
    hibernate:
      JDBC:
        time_zone: Asia/Bishkek
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          batch_size: 20
          fetch_size: 50
          order_inserts: true
          order_updates: true
          batch_versioned_data: true
          cache:
            use_second_level_cache: false
            use_query_cache: false

            # Статистика (выключить в продакшене)
            generate_statistics: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:migration

  jackson:
    time-zone: Asia/Bishkek
    date-format: yyyy-MM-dd'T'HH:mm:ss

server:
  port: 8080
  tomcat:
    threads:
      max: 200          # Максимум потоков
      min-spare: 10     # Минимум свободных потоков
    max-connections: 10000  # Максимум TCP соединений
    accept-count: 100       # Очередь запросов
    connection-timeout: 20000

jwt:
  secret: your-256-bit-secret-key-change-this-in-production
  expiration: 86400000 # 24 hours
  refresh-expiration: 604800000

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  prometheus:
    metrics:
      export:
        enabled: true

telegram:
  bot:
    token: 8288728857:AAFvrR2fDp9y1WQg-lv1CvCAO3A41N6rlBk

claude:
  api-key: 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
  model: claude-haiku-4-5-20251001

finik-withdraw:
  api-key: iFKEAzkc8f5wPrSVmnL6U60hYrhuqxBK8tZDS8jv
  account-id: 0090ec57-8f81-4d76-abc1-83dc08d076a5
  user-id: f69d4dce-8c75-4536-a4a5-19d845a5b1a1
  private-key-path: classpath:private_key.pem
  public-key-path: classpath:finik-public-key.pem
  base-url: https://api.paymentsgateway.averspay.kg
finik:
  api-key: LOlnU70w7LtRpBZ5HAxLaVatjIXQLGS9xmeSmWM4
  account-id: d54479fa-bea8-4762-ba96-6ec8b1eaea26
  private-key-path: classpath:workkg.pem
  base-url: https://api.acquiring.averspay.kg
  webhook-url: ${APP_BASE_URL}/api/bot/payments/finik

minio:
  url: https://fra1.digitaloceanspaces.com
  access-key: DO004G42KTU6VL83JA4Z
  secret-key: BajPpPGRgaLB9o1qD7+vZUg0pcBID/GGaiTqqTNCMB4
  bucket:
    resumes: resume-media
    vacancies: vacancy-media

logging:
  level:
    kg.workkg: DEBUG